import { ControlWrapperProps } from '../ControlWrapper';
import { СolorScheme } from '../types';
interface HintProps extends Partial<ControlWrapperProps> {
    hasErrors?: boolean;
    hintMargin?: number;
    variant?: СolorScheme;
}
export declare const Hint: import("styled-components").StyledComponent<"div", any, HintProps, never>;
export {};
