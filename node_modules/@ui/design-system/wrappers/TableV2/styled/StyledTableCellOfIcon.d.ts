import React from 'react';
import { IconProps } from '../../../icons_helpers/types';
export interface TableIconProps {
    /**
     * Иконка в ячейке таблицы.
     * Необходимо передать компонент иконки, импортированный отдельно в родительском элементе.
     */
    Icon: React.ComponentType<IconProps>;
    /**
     * Обработчик клика по иконке.
     */
    onClick?: () => void;
    /**
     * Состояние заблокированной иконки.
     * Если активно, не реагирует на нажатия и не вызывает обработчики событий.
     */
    disabled?: boolean;
}
export declare const StyledTableCellOfIcon: React.FC<TableIconProps>;
