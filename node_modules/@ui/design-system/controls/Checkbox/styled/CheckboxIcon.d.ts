import { CheckboxProps } from '../Checkbox';
export interface IconProps extends Partial<CheckboxProps> {
    checked: boolean;
}
export declare const CheckboxIcon: import("styled-components").StyledComponent<"span", any, IconProps, never>;
