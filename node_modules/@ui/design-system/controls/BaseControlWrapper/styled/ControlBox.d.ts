import { ControlWrapperProps } from '../ControlWrapper';
interface ControlBoxProps extends Partial<ControlWrapperProps> {
    hasErrors: boolean;
}
export declare const ControlBox: import("styled-components").StyledComponent<"div", any, ControlBoxProps, never>;
export {};
