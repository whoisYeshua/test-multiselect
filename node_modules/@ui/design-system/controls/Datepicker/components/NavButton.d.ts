export interface NavButtonProps {
    onClick: () => void;
    type: 'prev' | 'next';
    isDisabled?: boolean;
}
declare const NavButton: ({ onClick, type, isDisabled }: NavButtonProps) => JSX.Element;
export default NavButton;
