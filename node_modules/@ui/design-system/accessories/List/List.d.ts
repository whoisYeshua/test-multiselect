import React from 'react';
import { BadgeProps } from '../Badge/Badge';
export interface ListProps {
    /**
     * Тип списка: нумерованный либо ненумерованный.
     * Данный пропс определяет только тип списка для элементов верхнего уровня.
     * Тип списка для вложенных элементов задается внутри items.
     */
    listType?: 'ordered' | 'unordered';
    /**
     * Элементы списка. Внутри элемента может быть вложенный список. Каждый ListItem должен содержать уникальный id.
     * <a href="/?path=/story/design-system-справочник-сложные-типы-данных-в-компонентах--docs">Сложный тип данных</a>
     */
    items: ListItemProps[];
    /**
     * Цвет маркера для ненумерованного списка.
     */
    bulletVariant?: BadgeProps['variant'];
    /**
     * Дополнительные классы. Нужны для кастомизации стилей или селекторов для UI тестов.
     */
    className?: string;
}
export interface ListItemProps {
    /**
     * Текстовое содержимое элемента.
     */
    caption?: React.ReactNode;
    /**
     * Подзаголовок у элемента.
     */
    subcaption?: React.ReactNode;
    /**
     * Уникальный id у элемента.
     */
    itemId?: string;
    /**
     * Тултип в элементе списка.
     * При наведении на иконку вопроса показывает в доступном пространстве подсказку.
     */
    tooltip?: string;
    /**
     * Вложенный список внутри элемента.
     */
    innerList?: ListProps;
}
declare const List: React.FC<ListProps>;
export declare const defaultListProps: Partial<ListProps>;
export default List;
