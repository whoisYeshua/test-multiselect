import React from 'react';
import { SliderVariant } from '../Slider';
import { ButtonVariant } from '../../../accessories/Button';
interface ArrowProps {
    /**
     * Обработчик клика по ссылке.
     */
    onClick?: (event?: React.MouseEvent<HTMLButtonElement>) => void | Promise<void>;
    /**
     * Расположение кнопки переключения, справа либо слева.
     */
    right?: boolean;
    variant?: SliderVariant;
    buttonVariant?: ButtonVariant;
}
export declare const SliderArrow: ({ right, onClick, variant, buttonVariant }: ArrowProps) => JSX.Element;
export {};
