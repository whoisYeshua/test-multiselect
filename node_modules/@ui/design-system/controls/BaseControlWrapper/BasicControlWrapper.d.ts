import React from 'react';
import { ControlProps } from './ControlWrapper';
import { СolorScheme } from './types';
export interface BasicControlProps extends ControlProps {
    hintMargin?: number;
    isRangeInput?: boolean;
    withSubcaption?: boolean;
    /**
     * Вариант - от него зависят стили `Loader`, `Hint` и `Tooltip`.
     */
    variant?: СolorScheme;
    /**
     * Дочерний элемент, обёрнутый в контрол.
     */
    children?: React.ReactNode;
}
declare const BasicControlWrapper: {
    ({ hidden, locked, name, children, className, errors, errorIcon, tooltip, tooltipIconVariant, variant, caption, showTooltip, onTooltipClick, hint, disabled, hintMargin, id, isRangeInput, withSubcaption, }: BasicControlProps): JSX.Element;
    defaultProps: {
        isRangeInput: boolean;
        hidden: boolean;
        locked: boolean;
        variant: string;
    };
};
export default BasicControlWrapper;
