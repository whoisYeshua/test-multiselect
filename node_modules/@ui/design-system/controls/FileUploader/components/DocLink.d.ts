import React from 'react';
interface DocLinkProps {
    fileName: string;
    id?: string;
    url?: string;
    disabled?: boolean;
    fileLength?: number;
    detailedMode?: boolean;
    setErrors: React.Dispatch<React.SetStateAction<string[]>>;
    onFileRemove?: (id: string) => Promise<void>;
    onUploadRetry?: (name: string) => void;
    onUploadRetryCancel?: (name: string) => void;
    onFileDownload?: (id: string) => void;
    loading?: boolean;
    error?: boolean;
    errorText?: string;
}
declare const DocLink: ({ fileName, url, disabled, fileLength, id, loading, error, errorText, detailedMode, onFileRemove, onFileDownload, onUploadRetry, onUploadRetryCancel, setErrors, }: DocLinkProps) => JSX.Element;
export default DocLink;
