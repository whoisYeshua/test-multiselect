import { DadataSelectProps } from '../../DadataSelect/DadataSelect';
import { BankModelMapping } from '../types';
export interface BankDadataProps extends Omit<DadataSelectProps<'bank'>, 'onChange' | 'service' | 'type' | 'useFullItem'> {
    /**
     * Маппинг сущностей Dadata на поля вьюмодели
     */
    modelMapping?: BankModelMapping;
    /**
     * BANK - Банк, BANK_BRANCH - Филиал банка
     */
    bankType?: 'BANK' | 'BANK_BRANCH';
    /**
     * Обработчик изменений значения.
     */
    onChange: (props: BankDadataProps | string, newValue: string | null) => Promise<void> | void;
}
/**
 * Компонент, адаптированный для синхронизации ИРИС, для выбора значений из выпадающего списка банков,
 * полученного из dadata. После выбора заполняет поля в модели.
 */
declare const BankDadata: {
    (props: BankDadataProps): JSX.Element;
    defaultProps: {
        creatable: boolean;
        notFoundMessage: string;
        debounceTimer: number;
        acyncSearchMessage: string;
        optionsLoadingErrorMessage: string;
        disabled: boolean;
        locked: boolean;
        hidden: boolean;
        showTooltip: boolean;
        scrollTopOffset: number;
    };
    displayName: string;
};
export default BankDadata;
