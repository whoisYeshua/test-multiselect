import { FirstDayOfWeek } from '@datepicker-react/hooks';
import { ContextDayProps } from './Day';
/**
 * Пропсы для компонента месяца
 */
export interface MonthProps extends ContextDayProps {
    year: number;
    month: number;
    isRange?: boolean;
    startDate: Date;
    endDate: Date;
    unavailableDates: Date[];
    firstDayOfWeek: FirstDayOfWeek;
    isYearSelectorOpened?: boolean;
    doNotSelectPeriod: boolean;
}
declare const Month: ({ isDateSelected, isDateHovered, isFirstOrLastSelectedDate, isDateBlocked, isDateFocused, focusedDate, onDateHover, onDateSelect, onDateFocus, year, month, firstDayOfWeek, startDate, endDate, isRange, unavailableDates, doNotSelectPeriod, }: MonthProps) => JSX.Element;
export default Month;
