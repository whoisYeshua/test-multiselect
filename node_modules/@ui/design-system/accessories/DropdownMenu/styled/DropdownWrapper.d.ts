import { DropdownMenuProps } from '../DropdownMenu';
interface DropdownWrapperProps extends Partial<DropdownMenuProps> {
    isOpen: boolean;
    isVisible: boolean;
    leftPosition: string;
    rightPosition: string;
}
export declare const DropdownWrapper: import("styled-components").StyledComponent<"ul", any, DropdownWrapperProps, never>;
export {};
