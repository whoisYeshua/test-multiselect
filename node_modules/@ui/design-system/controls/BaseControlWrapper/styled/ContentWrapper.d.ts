import { ControlWrapperProps } from '../ControlWrapper';
interface ContentWrapperProps extends Partial<ControlWrapperProps> {
    isDelayedFocused: boolean;
}
export declare const ContentWrapper: import("styled-components").StyledComponent<"div", any, ContentWrapperProps, never>;
export {};
