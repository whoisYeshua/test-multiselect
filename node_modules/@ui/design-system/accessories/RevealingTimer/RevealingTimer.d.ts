/// <reference types="react" />
export interface RevealingTimerProps {
    /**
     * Время до показа содержимого в секундах.
     * Для рестарта таймера необходимо изменить время снаружи.
     * Самый простой путь: 1. Завести время в стейт. 2. Обнулять его в колбэке при окончании таймера.
     * 3. Снова указывать нужное время в стейте при необходимости перезапустить таймер.
     */
    time?: number;
    /**
     * Формы склонения минут.
     * По умолчанию используется винительный/родительный падежи: `['минуту', 'минуты', 'минут']`,
     * однако, в некоторых случаях может потребоваться указать другие формы.
     * Например: "Осталось 1 минутА"
     * Избегать <a href="/?path=/story/design-system-справочник-ссылочный-тип--docs">прямой передачи массива</a>
     */
    minutesForms?: string[];
    /**
     * Формы склонения секунд.
     * По умолчанию используется винительный/родительный падежи: `['секунду', 'секунды', 'секунд']`,
     * однако, в некоторых случаях может потребоваться указать другие формы.
     * Например: "Осталось 1 минутА и 21 секундА"
     * Избегать <a href="/?path=/story/design-system-справочник-ссылочный-тип--docs">прямой передачи массива</a>
     */
    secondsForms?: string[];
    /**
     * Возвращать ли единицы измерения (минуты и секунды).
     * По умолчанию возвращает.
     * При указании значения `false`, будет возвращать только значения минут и секунд в виде чисел.
     */
    withDimensions?: boolean;
    /**
     * Рендер-функция, возвращающая контент, который должен быть показан после окончания таймера.
     * Если функция рендера контента не передана, после окончания таймера ничего не будет отображаться.
     * <a href="/?path=/docs/design-system-справочник-рендер-функции--docs#рендер-функции-в-компонентах">Рендер-функция</a>
     */
    renderContent?: () => JSX.Element;
    /**
     * Функция обратного вызова, которая вызывается после окончания таймера.
     * Если функция обратного вызова не передана, момент окончания таймера не будет обработан,
     * однако контент всё равно будет отображен (если передан в параметре `renderContent`).
     */
    onCountdownEnd?: () => void;
    /**
     * Рендер-функция для отображения таймера оставшегося времени.
     * Должна обрабатывать 2 параметра - остаток минут и остаток секунд, форматируя их в зависимости от требований.
     * Данные о минутах и секундах форматируются в соответствии с параметрами `minutesForms` и `secondsForms`.
     * Например:
     * `(minutes, seconds) => <Typography>Отправить код повторно можно через ${minutes} ${seconds}</Typography>`.
     * В результате получится: `Отправить код повторно можно через 1 минуту 28 секунд`.
     * Если функция рендера таймера не передана, таймер нет будет отображен.
     * <a href="/?path=/docs/design-system-справочник-рендер-функции--docs#рендер-функции-в-компонентах">Рендер-функция</a>
     */
    renderCountdown: (minutes: string, seconds: string) => JSX.Element;
}
/**
 * Таймер обратного отсчёта, демонстрирующий время до отображения контента или выполнения определённого действия.
 */
declare const RevealingTimer: {
    ({ time, minutesForms, secondsForms, withDimensions, renderContent, renderCountdown, onCountdownEnd, }: RevealingTimerProps): JSX.Element;
    defaultProps: {
        time?: number;
        minutesForms?: string[];
        secondsForms?: string[];
        withDimensions?: boolean;
        renderContent?: () => JSX.Element;
        onCountdownEnd?: () => void;
        renderCountdown?: (minutes: string, seconds: string) => JSX.Element;
    };
    displayName: string;
};
export declare const defaultRevealingTimerProps: Partial<RevealingTimerProps>;
export default RevealingTimer;
