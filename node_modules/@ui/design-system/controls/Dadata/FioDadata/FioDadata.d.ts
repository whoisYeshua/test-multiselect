import { FioModelMapping, NullableStringField } from '../types';
import { DadataSelectProps } from '../../DadataSelect/DadataSelect';
export interface FioDadataProps extends Omit<DadataSelectProps<'fio'>, 'onChange' | 'service' | 'type' | 'useFullItem'> {
    /**
     * Маппинг сущностей Dadata на поля вьюмодели:
     * gender, name, patronymic, surname
     */
    modelMapping?: FioModelMapping;
    /**
     * Обработчик изменений значения.
     */
    onChange: (props: FioDadataProps | string, newValue: NullableStringField) => Promise<void> | void;
}
/**
 * Компонент, адаптированный для синхронизации ИРИС, для выбора значений из выпадающего списка фио,
 * полученного из dadata. После выбора заполняет поля в модели.
 */
declare const FioDadata: {
    (props: FioDadataProps): JSX.Element;
    defaultProps: {
        creatable: boolean;
        showDescription: boolean;
        notFoundMessage: string;
        debounceTimer: number;
        acyncSearchMessage: string;
        optionsLoadingErrorMessage: string;
        disabled: boolean;
        locked: boolean;
        hidden: boolean;
        showTooltip: boolean;
        size: import("../../BaseControlWrapper/styled/ControlBox").ControlBoxSize;
        scrollTopOffset: number;
    };
    displayName: string;
};
export default FioDadata;
