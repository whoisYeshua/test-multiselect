import React from 'react';
export interface FooterLink {
    name: string;
    url: string;
}
export interface LinksCategory {
    name: string;
    links: FooterLink[];
}
export interface FooterProps {
    /**
     * Набор ссылок для основных категорий в футере.
     * Указывать необходимо весь набор с разбиением по категориям.
     * Если требуется замена только одной ссылки из списка - остальные должны быть также указаны, но исходные.
     * Стандартный набор ссылок доступен для импорта, при необходимости можно ремаппить его.
     * <a href="/?path=/story/design-system-справочник-сложные-типы-данных-в-компонентах--docs">Сложный тип данных</a>
     */
    mainLinks?: LinksCategory[];
}
declare const Footer: React.ForwardRefExoticComponent<FooterProps & React.RefAttributes<HTMLDivElement>>;
export declare const defaultFooterProps: FooterProps;
export default Footer;
