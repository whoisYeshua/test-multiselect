import { CardProps } from '../Card';
interface HeaderWrapperProps extends Partial<CardProps> {
    hasButton: boolean;
}
export declare const HeaderWrapper: import("styled-components").StyledComponent<"div", any, HeaderWrapperProps, never>;
export {};
