import { ControlWrapperProps } from '../ControlWrapper';
import { RoundingSize } from '../../../helpers/styledHelpers';
export declare enum ControlBoxSize {
    L = "L",
    XS = "XS"
}
interface ControlBoxProps extends Partial<ControlWrapperProps> {
    hasErrors: boolean;
    multiLineCaption: boolean;
}
export declare const CONTROL_BOX_ROUNDINGS_MAP: {
    L: RoundingSize;
    XS: RoundingSize;
};
export declare const ControlBox: import("styled-components").StyledComponent<"div", any, ControlBoxProps, never>;
export {};
