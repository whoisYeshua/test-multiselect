import { RangeInputExternalProps } from './RangeInput';
declare type Scaler = {
    rangeInputWidth: number;
};
declare const Scaler: (props: Scaler & Partial<RangeInputExternalProps>) => JSX.Element;
export default Scaler;
