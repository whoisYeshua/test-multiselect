import React from 'react';
import { PortalProps } from '../Portal';
export interface TooltipProps {
    /**
     * Идентификатор тултипа в HTML разметке.
     * Если указывается, должен быть уникальным.
     * Используется в специализированной разметке для людей с ограниченными возможностями,
     * поэтому рекомендуется указывать всегда.
     */
    id?: string;
    /**
     * Текст, который отображается в подсказке.
     * Можно использовать JSX разметку, все стили текста, в таком случае, должны быть реализованы в приложении.
     */
    text: React.ReactNode;
    /**
     * Позволяет открыть тултип при помощи внешнего параметра.
     * При наведении на тултип и потере ховера он закроется как обычно
     * и для повторного открытия необходимо будет снова изменить параметр.
     */
    isOpen?: boolean;
    /**
     * Обработчик клика по иконке тултипа.
     * Возвращает все параметры тултипа, позволяя их обработать.
     * ВНИМАНИЕ! Даже при указании обработчика,
     * тултип автоматически отображается/скрывается при наведении/удалении курсора:
     * обработка клика позволяет расширить функциональность тултипа, но не заменяет стандартную.
     */
    onClick?: (props: TooltipProps) => void;
    /**
     * Обработчик "открытия" тултипа.
     * Срабатывает при наведении на иконку и показе контента, соответственно.
     */
    onShow?: (props: TooltipProps) => void;
    /**
     * Обработчик "закрытия" тултипа.
     * Срабатывает после потери ховера на иконку и после окончательного завершения анимации скрытия тултипа.
     */
    onHide?: (props: TooltipProps) => void;
    /**
     * Режим, при котором тултип отображается внутри ячейки таблицы и отображает все содержимое этой ячейки.
     * В этом режиме иконка тултипа не отображается, а сам тултип, по умолчанию, отображается снизу указанной зоны.
     */
    tableCellMode?: boolean;
    /**
     * Цвет иконки варианта default
     */
    color?: string;
    /**
     * Вариант внешнего вида иконки (с заливкой фона или без неё)
     */
    iconVariant?: 'filled' | 'default';
    /**
     * Цвет иконки с заливкой, если выбран этот тип
     */
    filledIconColor?: 'primary' | 'primary-fixed' | 'secondary' | 'tertiary' | 'on-color' | 'on-color-fixed' | 'brand-primary' | 'brand-secondary' | 'warning' | 'warning-contrast' | 'error' | 'error-contrast' | 'success' | 'success-contrast' | 'disabled' | 'link';
    /**
     * С ОСТОРОЖНОСТЬЮ!
     * Дополнительные свойства для настройки компонента `Portal`, который используется для отображения тултипа.
     * Позволяет переопределить стандартное поведение позиционирования, выравнивания и другие параметры.
     */
    portalProps?: PortalProps;
}
/**
 * Компонент для отображения всплывающей подсказки (появится по наведению на иконку)
 * Может присутствовать как в контроле, так и отдельно на странице
 */
declare const Tooltip: React.FC<TooltipProps>;
export declare const defaultTooltipProps: Partial<TooltipProps>;
export default Tooltip;
