import { DadataSelectProps, Service } from '../../DadataSelect/DadataSelect';
export interface DadataSelectUiCoreAdaptationProps<T extends Service = 'address'> extends Omit<DadataSelectProps<T, true>, 'value' | 'useFullItem'> {
    /**
     *  Выбранное значение селекта.
     */
    value: string | number | null;
}
/**
 * Компонент для выбора значений из выпадающего списка,
 * полученного из dadata, адаптированный для синхронизации ИРИС.
 */
declare const DadataSelectUiCoreAdaptation: {
    <T extends Service>(props: DadataSelectUiCoreAdaptationProps<T>): JSX.Element;
    defaultProps: {
        creatable: boolean;
        showDescription: boolean;
        notFoundMessage: string;
        debounceTimer: number;
        acyncSearchMessage: string;
        optionsLoadingErrorMessage: string;
        disabled: boolean;
        locked: boolean;
        hidden: boolean;
        showTooltip: boolean;
        size: import("../../BaseControlWrapper/styled/ControlBox").ControlBoxSize;
        scrollTopOffset: number;
    };
    displayName: string;
};
export default DadataSelectUiCoreAdaptation;
