/**
 * Функция-помощник для прокрутки страницы к первому полю с ошибкой.
 * Каждому контролу, для которого необходимо добавить прокрутку, в случае ошибки,
 * добавляется индивидуальный класс - список этих класов передается как аргумент в массиве selectors.
 * Саму функцию необходимо вызвать сразу после функции-валидатора, которая добавит к контролу класс-маркер ошибки.
 * Внимание!!! Функция использует `setTimeout` и возвращает его `id`.
 */
export declare const scrollToFirstError: (selectors: string[], selectorToScroll?: string, timeout?: number) => NodeJS.Timeout;
