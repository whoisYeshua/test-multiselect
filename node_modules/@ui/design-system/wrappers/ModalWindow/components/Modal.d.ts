/// <reference types="react" />
export interface ModalProps {
    children: JSX.Element;
    className: string;
}
declare const Modal: ({ children, className }: ModalProps) => import("react").ReactPortal;
export default Modal;
