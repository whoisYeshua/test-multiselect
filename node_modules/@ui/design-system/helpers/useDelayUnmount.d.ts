/**
 * Размонтирование компонента с задержкой.
 * Для ситуаций, когда нужно воспроизвести анимации исчезновения до того, как объект будет удалён из DOM
 */
export declare function useDelayUnmount(isMounted: boolean, delayTime: number, unmountHandler?: () => void): boolean;
