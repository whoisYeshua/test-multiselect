import React, { ReactNode } from 'react';
import { LinksCategory } from '../../accessories/Footer/Footer';
export interface CommonPageWrapperProps {
    /**
     * Дополнительное содержимое хедера. JSX разметка.
     * Применяется только при использовании стандартного хедера.
     */
    headerContent?: ReactNode;
    /**
     * Ссылка на главную по клику на логотип в хедере.
     * Применяется только при использовании стандартного хедера.
     */
    mainPageLink?: string;
    /**
     * Опциональный логотип партнёра для хедера. JSX разметка.
     * Размеры должны корректно стыковаться с основным лого.
     * Применяется только при использовании стандартного хедера.
     */
    partnerLogo?: React.ReactNode;
    /**
     * Набор ссылок для основных категорий в футере.
     * Указывать необходимо весь набор с разбиением по категориям.
     * Если требуется замена только одной ссылки из списка - остальные должны быть также указаны, но исходные.
     * Стандартный набор ссылок доступен для импорта из компонента `Footer`, при необходимости можно ремаппить его.
     * Применяется только при использовании стандартного футера.
     * <a href="/?path=/story/design-system-справочник-сложные-типы-данных-в-компонентах--docs">Сложный тип данных</a>
     */
    mainLinks?: LinksCategory[];
    /**
     * Опциональный хедер. JSX разметка.
     * Если указан, заменяет собой стандартный хедер целиком.
     * Вся внутренняя вёрстка и разметка остаётся на совести использующего.
     */
    customHeader?: React.ReactNode;
    /**
     * Опциональный футер. JSX разметка.
     * Если указан, заменяет собой стандартный футер целиком.
     * Вся внутренняя вёрстка и разметка остаётся на совести использующего.
     */
    customFooter?: React.ReactNode;
    /**
     * Основной контент страницы. JSX разметка.
     */
    children?: ReactNode;
}
/**
 * Универсальная обёртка для всех страниц, содержащая стандартные хедер и футер.
 * Может быть использована как на конкретных страницах (виджетах), так и на всё приложение целиком в самом корне,
 * сразу после провайдеров темы и размера экрана.
 */
declare const CommonPageWrapper: ({ children, headerContent, partnerLogo, mainPageLink, customHeader, customFooter, mainLinks, }: CommonPageWrapperProps) => JSX.Element;
export default CommonPageWrapper;
