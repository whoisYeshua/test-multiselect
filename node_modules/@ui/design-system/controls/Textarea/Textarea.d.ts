import React from 'react';
import { ExternalControlWrapperProps } from '../BaseControlWrapper/ControlWrapper';
export interface TextareaProps extends ExternalControlWrapperProps {
    /**
     * Введённое значение.
     */
    value?: string;
    /**
     * Плейсхолдер инпута, отображается когда на инпуте есть фокус, но ничего не введено.
     */
    placeholder?: string;
    /**
     * Определяет как часто будет генерироваться событие onChange
     */
    flushStrategy?: 'OnBlur' | 'OnEverySymbol';
    /**
     * Флаг запрета копирования текста из поля
     */
    preventCopy?: boolean;
    /**
     * Ограничитель максимальной высоты.
     * Применяется при перерасчёте высоты в зависимости от контента
     * и при привышении заданной высоты добавляет скролл вместо дальнейшего ресайза.
     * Значения меньше `52` будут игнорироваться, так как это минимально доступное ограничение высоты для `textarea`.
     */
    maxHeight?: number;
    /**
     * Обработчик изменений значения.
     */
    onChange?: (props: TextareaProps, newValue: string) => Promise<void> | void;
    /**
     * Обработчик события начала редактирования значения. Вызывается по фокусу.
     */
    onEditStart?: (props: TextareaProps) => Promise<void> | void;
    /**
     * Обработчик события завершения редактирования значения. Вызывается после потери фокуса.
     */
    onEditEnd?: (props: TextareaProps) => Promise<void> | void;
}
/**
 * Поле ввода для объемных текстов.
 */
declare const Textarea: React.FC<TextareaProps>;
export declare const defaultTextareaProps: Partial<TextareaProps>;
export default Textarea;
