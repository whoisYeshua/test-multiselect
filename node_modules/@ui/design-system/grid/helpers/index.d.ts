import { ScreenWidthShortcut } from '@ui/screen-size-provider';
import { GridMargins } from '../components/Column';
export declare const calcColumnWidth: (maxCols: number, cols: number, extraValue?: number) => string;
export declare const calcColOffset: (maxCols: number, cols: number, offset: number, extraValue?: number) => string;
export declare const getMaxColsWithLimits: (maxCols: number) => number;
export declare const calculateMagrins: <T extends GridMargins>(props: T, breakpoint: ScreenWidthShortcut) => string;
