import { Status, Variant } from '../StepsProgressBar';
declare type NumberType = {
    status: Status;
    isClickable: boolean;
    variant: Variant;
};
export declare const NumberComponent: import("styled-components").StyledComponent<"div", any, NumberType, never>;
export {};
