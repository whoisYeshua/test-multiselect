import { LoaderProps, MessageParams } from '../Loader';
export declare type MessageWrapperParams = LoaderProps & MessageParams & {
    hasCustomSpinner: boolean;
};
declare const Message: ({ header, subheader, isInControl, hasCustomSpinner }: MessageWrapperParams) => JSX.Element;
export default Message;
