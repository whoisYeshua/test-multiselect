import React from 'react';
import { GridMargins } from './Column';
export interface RowProps extends GridMargins {
    /**
     * Дочерний элемент или группа элементов
     */
    children?: React.ReactNode;
    /**
     * Дополнительные классы. Нужны для кастомизации стилей или селекторов для UI тестов.
     */
    className?: string;
    /**
     * Скрытие строки. Удаляет строку вместе со всем содержимым из разметки во время рендера.
     */
    hidden?: boolean;
    /**
     * Идентификатор строки в DOM.
     */
    id?: string;
}
/**
 * Строка разметки сетки контента.
 */
declare const Row: {
    ({ children, className, mtMobile, mtTablet, mtDesktop, mbMobile, mbTablet, mbDesktop, hidden, id, }: RowProps): JSX.Element;
    defaultProps: {
        mtMobile: number;
        mbMobile: number;
        hidden: boolean;
    };
    displayName: string;
};
export declare const defaultRowProps: {
    mtMobile: number;
    mbMobile: number;
    hidden: boolean;
};
export default Row;
