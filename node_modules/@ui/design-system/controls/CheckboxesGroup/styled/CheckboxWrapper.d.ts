import { Direction } from '../../../types';
interface CheckboxWrapper {
    direction: Direction;
    itemsMargin: number;
}
export declare const CheckboxWrapper: import("styled-components").StyledComponent<"div", any, CheckboxWrapper, never>;
export {};
