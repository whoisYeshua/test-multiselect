import React, { ReactElement } from 'react';
import { ControlProps } from '../BaseControlWrapper/ControlWrapper';
import { Direction } from '../../types';
import { CheckBoxGroupItem } from '../CheckboxesGroup';
import { OnChangeGroupItemValue } from '../CheckboxesGroup/CheckboxesGroup';
declare type SelectButtonGroupItem = Omit<CheckBoxGroupItem, 'variant'>;
export interface SelectButtonGroupProps<Item extends SelectButtonGroupItem = SelectButtonGroupItem> extends ControlProps {
    /**
     * Задаёт имя группы (group) для связывания input-ов.
     */
    name: string;
    /**
     * Список со значениями.
     * <a href="/?path=/story/design-system-справочник-сложные-типы-данных-в-компонентах--docs">Сложный тип данных</a>
     */
    items: Item[];
    /**
     * Направление размещения элементов.
     * Колонкой сверху вниз или строкой слева направо.
     * В режиме строки необходимо внимательно относиться к доступной ширине экрана и к адаптивности.
     */
    direction?: Direction;
    /**
     * Обработчик изменений значения.
     */
    onChange?: (props: SelectButtonGroupProps<Item>, code: OnChangeGroupItemValue<Item>) => Promise<void> | void;
    /**
     * Обработчик события начала редактирования значения. Вызывается по фокусу.
     */
    onEditStart?: (props: SelectButtonGroupProps<Item>) => Promise<void> | void;
    /**
     * Обработчик события завершения редактирования значения. Вызывается после потери фокуса.
     */
    onEditEnd?: (props: SelectButtonGroupProps<Item>) => Promise<void> | void;
    /**
     * Функция для изменения отрисовки лейбла кнопки.
     * <a href="/?path=/docs/design-system-справочник-рендер-функции--docs#рендер-функции-в-компонентах">Рендер-функция</a>
     */
    renderCustomLabel?: (item: Item, checked?: boolean) => React.ReactNode;
}
export declare const defaultSelectButtonGroupProps: Partial<SelectButtonGroupProps>;
export declare type SelectButtonGroupType = <Item extends SelectButtonGroupItem>(props: SelectButtonGroupProps<Item>) => ReactElement;
declare const _default: SelectButtonGroupType;
export default _default;
