import React from 'react';
import { ScreenWidthShortcut } from '../../../types';
import { TableProps } from '../TableV2';
interface InnerProps {
    itemsPerPage: TableProps['itemsPerPage'];
    isPaginationAvailable: TableProps['isPaginationAvailable'];
    headersData: TableProps['headersData'];
    columns: any[];
    rowData: any[];
    centeredColumns: number[];
    disablePaginationPageSelector: boolean;
    disablePaginationPageCount: boolean;
    paginationButtonText: boolean;
    mobilePaginator: boolean;
    screenWidthShortcut?: ScreenWidthShortcut;
}
export declare const Table: React.FC<InnerProps>;
export {};
