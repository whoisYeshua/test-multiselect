import React from 'react';
import { ScreenWidthShortcut } from '../../types';
export interface HeaderProps {
    /**
     * Дополнительное содержимое хедера. JSX разметка.
     */
    children?: React.ReactNode;
    /**
     * Ссылка на главную по клику на логотип.
     */
    mainPageLink?: string;
    /**
     * Опциональный логотип партнёра.
     * Необходимо учитывать размеры для корректного отображения.
     */
    partnerLogo?: React.ReactNode;
    /**
     * Разрешение экрана в виде строкового тега. Передается автоматически из контекста размера экрана.
     */
    screenWidthShortcut?: ScreenWidthShortcut;
}
/**
 * Стандартный header для страницы.
 */
declare const Header: ({ screenWidthShortcut, mainPageLink, partnerLogo, children }: HeaderProps) => JSX.Element;
export default Header;
