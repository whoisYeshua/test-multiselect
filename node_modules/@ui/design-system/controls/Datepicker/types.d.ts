import { FocusedInput } from '@datepicker-react/hooks';
export interface DateData {
    startDate: string | null;
    endDate: string | null;
}
export interface DatepickerState extends DateData {
    focusedInput: FocusedInput;
}
export interface MonthItemArgs {
    currentYear: number;
    minYear: number;
    maxYear: number;
    minMonth: number;
    maxMonth: number;
}
