import React from 'react';
interface DocLinkProps {
    fileName: string;
    id?: string;
    url?: string;
    disabled?: boolean;
    fileLength?: number;
    detailedMode?: boolean;
    setErrors: React.Dispatch<React.SetStateAction<string[]>>;
    onFileRemove?: (id: string) => Promise<void>;
    onUploadRetry?: (name: string) => void;
    onFileDownload?: (id: string) => void;
    loading?: boolean;
    error?: boolean;
}
declare const DocLink: ({ fileName, url, disabled, fileLength, id, loading, error, detailedMode, onFileRemove, onFileDownload, onUploadRetry, setErrors, }: DocLinkProps) => JSX.Element;
export default DocLink;
