interface StyledExternalLinkIconProps {
    size: number;
}
export declare const StyledExternalLinkIcon: import("styled-components").StyledComponent<({ color, variant, ...props }: import("../../../icons_helpers/types").IconProps) => JSX.Element, any, {
    color: "var(--text-link)";
    width: `${number}px`;
    height: `${number}px`;
} & StyledExternalLinkIconProps, "color" | "width" | "height">;
export {};
