import React from 'react';
import { AlertCommonProps, AlertType } from '../../accessories/Alert/Alert';
export declare type PositionType = 'static' | 'absolute' | 'fixed';
export interface AlertContainerProps extends AlertCommonProps {
    /**
     * Массив с алертами.
     * <a href="/?path=/story/design-system-справочник-сложные-типы-данных-в-компонентах--docs">Сложный тип данных</a>.
     * Содержимое массива передается в компонент `Alert` и будет иметь приоритет
     * над некоторыми параметрами коммпонента `AlertContainer`.
     */
    alerts: AlertType[];
    /**
     * Определяет будет ли рендериться кнопка закрытия для алерта.
     * Если она рендерится, то алерт закрывается только по клику на нее.
     * В противном случае, алерт можно закрыть кликом по самому алерту.
     * Применяется для всех алертов.
     * Этот параметр так-же доступен индивидуально для каждого алерта в контейнере и имеет приоритет.
     */
    shouldRenderCloseButton?: boolean;
    /**
     * Таймаут закрытия алерта в мс. Если установлено значение false,
     * алерт необходимо закрыть вручную (по клику или через редакс-стор).
     * Не работает, если алерт используется без AlertContainer.
     * Применяется для всех алертов.
     * Этот параметр так-же доступен индивидуально для каждого алерта в контейнере и имеет приоритет.
     */
    autoClose?: boolean | number;
    /**
     * Добавлять анимацию при закрывании алерта. По умолчанию - да.
     */
    withAnimation?: boolean;
    /**
     * Тип позиционирования
     */
    position?: PositionType;
    /**
     * Позиционирование алерта сверху
     */
    top?: string;
    /**
     * Позиционирование алерта справа
     */
    right?: string;
}
/**
 * Контейнер для алертов.
 * Позиционирует алерты и добавляет возможность их самостоятельного закрытия по таймауту.
 * Его можно подключить к редакс-стору из
 * [@ui/core](https://git.mos.renins.com/iris/front/ui-core/-/tree/dev/src/BaseWidget/alerts)
 * или к любому другому стору.
 * При закрытии по клику необходимо удалить алерт из массива передаваемых алертов.
 */
declare const AlertContainer: React.FC<AlertContainerProps>;
export declare const defaultAlertContainerProps: Partial<AlertContainerProps>;
export default AlertContainer;
