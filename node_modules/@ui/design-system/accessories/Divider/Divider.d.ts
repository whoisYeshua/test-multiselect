import React from 'react';
export interface DividerProps {
    /**
     * Текстовое содержимое компонента.
     */
    textContent?: string;
    /**
     * Расположение текста в компоненте.
     */
    textPosition?: 'start' | 'center' | 'end' | 'none';
    /**
     * Ориентация строки с текстом - горизонтальная либо вертикальная. При вертикальном расположении необходимо
     * задать высоту родительскому контейнеру для определения высоты декоративных линий.
     * При горизонтальном расположении ширина по умолчанию 100%, но также может корректироваться.
     */
    orientation?: 'horizontal' | 'vertical';
    /**
     * Дополнительные классы. Нужны для кастомизации стилей или селекторов для UI тестов.
     */
    className?: string;
}
/**
 * Компонент для добавления тексту декоративных границ по бокам. Возможно также использование без текстового
 * содержимого в режиме обычной границы.
 */
declare const Divider: React.FC<DividerProps>;
export declare const defaultDividerProps: Partial<DividerProps>;
export default Divider;
